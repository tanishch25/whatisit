import sharp from 'sharp'; import fs from 'fs'; import path from 'path'; const dir = './src/assets'; const files = fs.readdirSync(dir).filter(f => f.endsWith('.jpg')); for (const f of files) { const inP = path.join(dir, f); const outP = path.join(dir, f.replace('.jpg', '.webp')); await sharp(inP).resize(800, null, {withoutEnlargement: true}).webp({quality: 60}).toFile(outP); fs.unlinkSync(inP); console.log('Done ' + f); }
